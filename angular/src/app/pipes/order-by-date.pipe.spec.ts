import { OrderAnnonceByDatePipe } from './order-by-date.pipe';

describe('OrderAnnonceByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderAnnonceByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
